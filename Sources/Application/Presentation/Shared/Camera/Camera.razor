@using Microsoft.AspNetCore.Components.Forms
@using MudBlazor
<InputFile OnChange="OnFileSelectedAsync" accept="image/*" capture="environment"/>

<label for="cameraInput">
    <MudButton>Foto aufnehmen</MudButton>
</label>

<InputFile id="cameraInput"
           style="opacity:0; position:absolute; width:0; height:0;"
           OnChange="OnFileSelectedAsync"
           accept="image/*"
           capture="environment"/>


<p>
    @_imageDataUrl
</p>